# Archive Reuse Foundation

The archive is a searchable asset library. Active runtime reads manifests only. P0 rows are blocked, direct imports from archived source are forbidden, and future reuse must go through adapters that preserve V1.2 boundaries.
