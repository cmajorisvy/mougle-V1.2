# User Agent Micro-Pyramid

The Micro-Pyramid computes LocalReadiness and allowed local action classes only: proceed_local, ask_user, simulate_more, escalate_to_council, block, and archive. It never returns `publish_truth` and never writes Stage 4 or Stage 1.
