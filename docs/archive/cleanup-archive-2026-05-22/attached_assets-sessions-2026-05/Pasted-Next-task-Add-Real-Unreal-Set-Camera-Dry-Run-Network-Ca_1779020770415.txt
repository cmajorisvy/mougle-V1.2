Next task: Add Real Unreal Set-Camera Dry-Run Network Call.

Goal:
Enable a dry-run-only set_camera network call to the configured Unreal bridge. This must only send a sanitized camera-selection summary and must not load levels, render scenes, import assets, attach avatars, attach media, start Sequencer, trigger Movie Render Queue, send 4D commands, or publish anything.

Important:
Do not enable live Unreal commands.
Do not render video.
Do not trigger Movie Render Queue.
Do not import assets.
Do not load Unreal levels.
Do not attach avatars.
Do not attach video panels.
Do not start Sequencer.
Do not send 4D hardware commands.
Do not publish anything.
realSendAllowed must remain false.

Scope:

1. Add or extend a section in /admin/production-house:
Real Unreal Set-Camera Dry-Run

Features:
- select production
- require approval stage: unreal_sandbox_approved
- require latest successful prepare_scene dry-run record
- choose camera preset from allowed list
- generate sanitized set_camera dry-run payload
- send dry-run set_camera request
- view sanitized request summary
- view sanitized bridge response
- view set-camera history

2. Allowed network call:
POST {UNREAL_BRIDGE_BASE_URL}/set-camera/dry-run

Payload must be allowlist-only:
{
  "commandType": "set_camera",
  "mode": "dry_run",
  "productionId": "...",
  "dryRun": true,
  "realSendAllowed": false,
  "safetyEnvelope": SAFETY_ENVELOPE,
  "source": "mougle-production-house",
  "timestamp": "...",
  "cameraSummary": {
    "cameraPreset": "...",
    "approvalStage": "unreal_sandbox_approved",
    "hasPrepareSceneDryRunPassed": true,
    "internalOnly": true,
    "visibility": "admin_only_internal",
    "publicUrlsPresent": false,
    "signedUrlsPresent": false,
    "renderRequested": false,
    "levelLoadRequested": false,
    "sequencerStartRequested": false
  }
}

3. Allowed camera presets:
- anchor_closeup
- anchor_medium
- wide_newsroom
- podcast_two_shot
- debate_wide
- hall_stage_wide
- product_reveal
- market_wall
- emergency_broadcast
- custom_static

4. Headers:
- Authorization: Bearer UNREAL_BRIDGE_TOKEN
- Content-Type: application/json

Never log or return the token.

5. Requirements:
- root-admin only
- UNREAL_BRIDGE_BASE_URL required
- UNREAL_BRIDGE_TOKEN required
- UNREAL_BRIDGE_MODE must equal "dry_run"
- confirm:true required
- production must be unreal_sandbox_approved
- latest prepare_scene dry-run record must have status passed
- request timeout required
- endpoint path must be exactly /set-camera/dry-run
- response must be sanitized before storage

6. Add endpoint:
POST /api/admin/production-house/real-unreal/set-camera-dry-run/:productionId/send

7. Add status/history endpoints:
GET /api/admin/production-house/real-unreal/set-camera-dry-run/status
GET /api/admin/production-house/real-unreal/set-camera-dry-run/history

8. Add storage:
realUnrealSetCameraDryRunHistory collection.

9. Store set-camera record:
- id
- productionId
- mode:"dry_run"
- commandType:"set_camera"
- cameraPreset
- endpointHost only
- endpointPath:"/set-camera/dry-run"
- status:"passed" | "failed" | "rejected"
- httpStatus nullable
- realSendAllowed:false
- requestSummary
- responseSummary
- createdAt
- safetyEnvelope

10. Add audit events:
- real_unreal.set_camera.status_viewed
- real_unreal.set_camera.attempted
- real_unreal.set_camera.passed
- real_unreal.set_camera.failed
- real_unreal.set_camera.rejected
- real_unreal.set_camera.history_viewed

11. Add UI:
- production selector
- camera preset selector
- status panel
- set-camera dry-run button
- confirmation prompt
- sanitized payload preview
- sanitized response viewer
- history table
- warning:
  “This sends only a dry-run set_camera summary. It does not load levels, render scenes, import assets, attach media, start Sequencer, or execute real Unreal commands.”

12. Add export support:
- include set-camera dry-run history as metadata only
- no token
- no full bridge URL
- no public URLs
- no signed URLs
- no provider secrets

13. Add regression tests:
- status endpoint requires root-admin
- send endpoint requires root-admin
- history endpoint requires root-admin
- send requires confirm:true
- send rejects missing bridge config
- send rejects mode other than dry_run
- send rejects unknown productionId
- send rejects production not unreal_sandbox_approved
- send rejects when latest prepare_scene dry-run did not pass
- send rejects unsupported camera preset
- endpoint path is exactly /set-camera/dry-run
- payload contains dryRun:true
- payload contains realSendAllowed:false
- payload contains no render/import/load-level/sequencer/MRQ commands
- payload contains no asset files, provider URLs, API keys, or bridge token
- response is sanitized before storage
- timeout failure is recorded safely
- HTTP 4xx/5xx is recorded safely
- history persists through storage adapter
- export includes set-camera metadata but no secrets
- no 4D command is sent
- SAFETY_ENVELOPE unchanged

14. Preserve all existing Production House tests.

Do not add live Unreal command sending in this task.
Do not add real 4D hardware integration.