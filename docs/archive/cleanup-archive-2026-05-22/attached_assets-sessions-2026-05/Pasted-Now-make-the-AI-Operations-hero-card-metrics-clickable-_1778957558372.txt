Now make the AI Operations hero card metrics clickable on the Mougle founder/admin dashboard.

Context:
The project already has:

1. Founder dashboard AI Operations hero card:
   - AiOpsHeroCard.tsx

2. Admin AI Ops Summary:
   - GET /api/admin/ai-ops/summary

3. Admin AI Jobs page with URL filters:
   - /admin/ai-jobs?status=...
   - /admin/ai-jobs?status=failed&since=24h
   - /admin/ai-jobs?lockedBy=...

4. Admin AI Workers page with URL filters:
   - /admin/ai-workers?derivedStatus=offline
   - /admin/ai-workers?status=unhealthy
   - /admin/ai-workers?workerId=...

5. Admin AI Retention page with URL filters:
   - /admin/ai-retention?section=history
   - /admin/ai-retention?status=failed&section=history

6. CSV export audit logging:
   - GET /api/admin/ai-export-events

Goal:
Make the AI Operations hero card numbers themselves clickable so admins can jump directly to filtered operational views.

Do not rewrite the dashboard.
Do not add backend persistence.
Do not add Redis.
Do not add cron.
Do not expose Python directly to the frontend.
No Python changes needed.

Part A — Clickable hero metrics

Update the AI Operations hero card so these metric tiles/numbers are links:

1. Pending jobs
   Link:
   /admin/ai-jobs?status=pending

2. Running jobs
   Link:
   /admin/ai-jobs?status=running

3. Failed jobs last 24h
   Link:
   /admin/ai-jobs?status=failed&since=24h

4. Workers online
   Link:
   /admin/ai-workers?derivedStatus=online

5. Stale/offline workers
   Link:
   /admin/ai-workers?derivedStatus=offline

6. Nudges / notifications
   Link:
   /admin/ai-ops

Optional if visible in the card:

7. Cleanup eligible / retention backlog
   Link:
   /admin/ai-retention?section=history

8. CSV exports today or export history if shown
   Link:
   /admin/ai-retention?section=exports

Part B — UX requirements

1. Keep the card compact.
2. Make links visually clear but not noisy.
3. Add accessible labels or title attributes where helpful.
4. Preserve existing quick links.
5. Do not make the card look like a table unless that matches existing style.
6. Empty/zero metrics can still be clickable if useful.
7. Avoid nested anchors if the card already has link wrappers.
8. Keep hover/focus styling consistent with the app.

Part C — Validation

Run existing checks/tests.

Confirm:
- founder/admin dashboard still loads.
- pending metric links to /admin/ai-jobs?status=pending.
- running metric links to /admin/ai-jobs?status=running.
- failed 24h metric links to /admin/ai-jobs?status=failed&since=24h.
- workers online metric links to /admin/ai-workers?derivedStatus=online.
- stale/offline metric links to /admin/ai-workers?derivedStatus=offline.
- nudges metric links to /admin/ai-ops.
- no backend changes are required.
- no Python changes are required.

After changes, summarize:
- files created
- files modified
- metrics made clickable
- destination URLs
- validation results
- what remains for the next step