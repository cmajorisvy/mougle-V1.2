Create a downloadable ZIP package for only the Mougle Production House unit so I can test it separately in Codex.

Goal:
Collect all files required to inspect, test, and debug:
/admin/production-house
and its backend APIs under:
/api/admin/production-house/*

Do not include secrets.
Do not include node_modules.
Do not include database dumps.
Do not include client/public/downloads.
Do not include .env files.
Do not include API keys.
Do not include build artifacts unless needed for reference.

Create a ZIP file named:
mougle-production-house-unit.zip

Include these files and folders if they exist:

Frontend:
- client/src/pages/admin/ProductionHouse.tsx
- client/src/pages/admin/PreviewStudioHero.tsx
- any client/src/pages/admin/production-house/*
- any client/src/components/production-house/*
- any related tooltip, badge, preview, wizard, room, avatar, media, Unreal, or 4D components

Shared schemas:
- shared/production-house.ts
- any shared/models related to production-house, preview studio, Unreal dry-run, 4D sandbox, or media package safety

Backend routes:
- server/routes/production-house-routes.ts
- server/routes/preview-studio-routes.ts
- any server/routes files that mount /api/admin/production-house
- relevant portions or full file of server/routes.ts if it registers the production-house routes

Backend services:
- server/services/production-house-service.ts
- server/services/production-house-storage.ts
- server/services/preview-studio-service.ts
- any server/services files related to:
  - OpenAI production generation
  - ElevenLabs voice
  - Meshy assets
  - Runway video
  - Asset Library
  - Package Viewer
  - Readiness Center
  - Approval Board
  - Unreal sandbox
  - Unreal bridge contract
  - Real Unreal dry-run
  - 4D sandbox
  - Preview Studio
  - Production Wizard
  - Media Pipeline
  - Room Generator
  - Avatar Creator

Tests:
- tests/production-house.test.ts
- tests/preview-studio.test.ts
- any tests related to production-house, preview-studio, Unreal dry-run, 4D sandbox, wizard, asset library, package viewer, or safety gates

Config:
- package.json
- package-lock.json or pnpm-lock.yaml or yarn.lock
- tsconfig.json
- vite.config.ts
- tailwind.config.ts
- postcss.config.js
- components.json if shadcn/ui is used
- replit.md if it documents the production-house unit
- .gitignore

Reports/docs:
- docs/reports/*PRODUCTION_HOUSE*
- docs/reports/*PREVIEW_STUDIO*
- docs/reports/*UNREAL*
- docs/reports/*4D*
- docs/reports/*PR55* only if relevant to current blockers

Also create a manifest file inside the ZIP:
PRODUCTION_HOUSE_FILE_MANIFEST.md

The manifest should include:
1. List of included files
2. Purpose of each major file
3. Routes included
4. Tests included
5. Known issues
6. How to run tests
7. How to start local dev
8. Safety assumptions:
   - realSendAllowed must remain false
   - executionEnabled must remain false
   - no real Unreal commands
   - no real 4D hardware
   - no publishing
   - no publicUrl or signedUrl except null

Also create:
PRODUCTION_HOUSE_TEST_PLAN.md

The test plan should include:
1. API route tests
2. Preview Studio tests
3. UI smoke tests
4. Root-admin gating tests
5. Safety invariant tests
6. Export tests
7. Missing route 404 JSON tests
8. Suggested Codex debugging checklist

Before creating the ZIP, run:
- rg "production-house|preview-studio|PreviewStudio|ProductionHouse|real-unreal|fourD|4D|wizard|readiness|approval" client/src server shared tests docs config package.json
to discover all relevant files.

Then create the ZIP in a downloadable location, for example:
downloads/mougle-production-house-unit.zip
or
/tmp/mougle-production-house-unit.zip

After creating the ZIP, provide a direct download link in the Replit UI.