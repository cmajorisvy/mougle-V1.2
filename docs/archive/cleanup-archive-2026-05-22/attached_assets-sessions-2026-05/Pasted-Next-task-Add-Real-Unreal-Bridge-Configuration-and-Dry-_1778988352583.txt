Next task: Add Real Unreal Bridge Configuration and Dry-Run Handshake to Mougle AI Production House.

Goal:
Prepare the platform for future real Unreal Remote Control integration by adding a root-admin configuration page, bridge credential checks, and dry-run-only handshake workflow. This must not send real Unreal commands yet.

Important:
Do not enable real Unreal commands.
Do not trigger Movie Render Queue.
Do not import assets into Unreal.
Do not render video.
Do not send 4D hardware commands.
Do not publish anything.
realSendAllowed must remain false.
This task is configuration, validation, dry-run handshake, UI, audit, and tests only.

Scope:

1. Add new section in /admin/production-house:
Real Unreal Bridge Setup

Features:
- view bridge configuration status
- show required environment variables
- test dry-run bridge handshake
- validate bridge contract compatibility
- view handshake history
- show safety badges:
  Dry Run Only
  Real Send Disabled
  No Render
  No Asset Import
  No 4D Send
  No Publishing

2. Required environment variables:
UNREAL_BRIDGE_BASE_URL
UNREAL_BRIDGE_TOKEN
UNREAL_BRIDGE_MODE

UNREAL_BRIDGE_MODE must only allow:
- disabled
- dry_run

Do not allow live or real mode in this task.

3. Add backend endpoints:
GET /api/admin/production-house/real-unreal/setup/status
POST /api/admin/production-house/real-unreal/setup/handshake-dry-run
GET /api/admin/production-house/real-unreal/setup/handshake-history
POST /api/admin/production-house/real-unreal/setup/validate-config

All routes must be root-admin gated.

4. Status endpoint should return booleans only:
- configured
- hasBaseUrl
- hasToken
- mode
- dryRunOnly:true
- realSendAllowed:false

Never return UNREAL_BRIDGE_TOKEN or any secret value.

5. Dry-run handshake should:
- require UNREAL_BRIDGE_BASE_URL
- require UNREAL_BRIDGE_TOKEN
- require UNREAL_BRIDGE_MODE="dry_run"
- require explicit confirm:true
- send only a dry-run health_check payload if a network call is implemented
- include dryRun:true
- include realSendAllowed:false
- include SAFETY_ENVELOPE
- never send production package data
- never send asset data
- never send render commands
- never send import commands

6. If external network call is implemented, allow only:
POST {UNREAL_BRIDGE_BASE_URL}/health/dry-run

Payload:
{
  "commandType": "health_check",
  "mode": "dry_run",
  "dryRun": true,
  "realSendAllowed": false,
  "safetyEnvelope": SAFETY_ENVELOPE,
  "timestamp": "...",
  "source": "mougle-production-house"
}

7. Store handshake result:
- id
- mode:"dry_run"
- endpointHost only, not full secret URL if sensitive
- status:"dry_run_ok" | "dry_run_failed" | "config_missing" | "rejected"
- realSendAllowed:false
- requestSummary
- responseSummary
- createdAt
- safetyEnvelope

8. Add storage:
realUnrealHandshakeHistory collection.

9. Add audit events:
- real_unreal.setup.status_viewed
- real_unreal.setup.config_validated
- real_unreal.setup.handshake_attempted
- real_unreal.setup.handshake_succeeded
- real_unreal.setup.handshake_failed
- real_unreal.setup.handshake_rejected
- real_unreal.setup.history_viewed

10. Add UI:
- configuration status cards
- dry-run handshake button
- config validation panel
- handshake response viewer
- handshake history table
- clear warning:
  “This page does not enable real Unreal commands. It only verifies dry-run bridge readiness.”

11. Add export support:
- include realUnrealHandshakeHistory in full production package export only as metadata
- no secrets
- no full token
- no public URLs
- no signed URLs

12. Add regression tests:
- all real-unreal setup endpoints require root-admin
- status returns booleans only and never token
- status has realSendAllowed:false
- live/real mode is rejected
- dry-run handshake requires confirm:true
- dry-run handshake rejects missing config
- dry-run handshake never sends production package data
- dry-run handshake never sends render/import commands
- handshake history persists through storage adapter
- export includes handshake metadata but no secrets
- no real Unreal command is sent
- no 4D command is sent
- SAFETY_ENVELOPE unchanged

13. Preserve all existing tests.

Do not add live Unreal command sending in this task.
Do not add real 4D hardware integration in this task.