Next task: Add Runway AI Video / B-roll Draft Jobs to Mougle AI Production House.

Goal:
Allow root-admin users to create mock and real Runway video draft jobs for newsroom screen content, podcast intros, B-roll, cinematic transitions, LED wall loops, explainer visuals, and background video panels.

Important:
Mock mode must remain default.
Do not enable real Unreal sends.
Do not enable real 4D hardware sends.
Do not enable publishing.
Do not expose secrets.
Do not auto-approve any video or production.
Do not import videos into Unreal yet.

Scope:

1. Add Runway availability endpoint:
GET /api/admin/production-house/video/runway/availability

Return booleans only:
- available
- hasCredential
- mockMode
- realSendAllowed:false

Never return RUNWAY_API_KEY or any secret value.

2. Add mock video generation route:
POST /api/admin/production-house/video/runway/generate-mock

Mock mode should:
- not call external APIs
- generate deterministic mock video job metadata using SHA-256
- work without RUNWAY_API_KEY
- remain available for tests and local development

3. Add real Runway draft job route:
POST /api/admin/production-house/video/runway/generate

Requirements:
- root-admin only
- requires RUNWAY_API_KEY
- requires explicit confirm:true
- validates productionId or standalone video prompt
- validates video type
- audit-logs every attempt
- saves output as a draft video job only
- does not approve the production
- does not send anything to Unreal
- does not send anything to 4D hardware
- does not publish videos publicly

4. Add video job model/storage through the existing ProductionHouseStorage adapter.

Video job fields:
- id
- productionId nullable
- provider: mock | runway
- videoType: newsroom_screen | podcast_intro | broll | transition | led_wall | explainer | background_loop | custom
- prompt
- promptHash
- durationSeconds
- aspectRatio
- status: draft | submitted | generated | failed
- approvalStatus: draft
- providerJobId nullable
- internalVideoPath nullable
- videoUrl:null
- publicUrl:null
- signedUrl:null
- visibility:admin_only_internal
- createdAt
- metadata
- safetyEnvelope

5. If real Runway returns video URLs, do not expose them as public production URLs.
Store them only as internal provider metadata, unless the schema requires null for public-facing URLs.

6. Add UI section:
Video Studio

Features:
- select production
- choose video type
- enter video prompt
- choose duration
- choose aspect ratio
- provider selector: Mock / Runway
- generate draft video job
- show video job cards
- show status badges:
  Draft
  Internal Only
  No Public URL
  No Unreal Import
  No 4D Send

7. Add export support:
- include videoJobs in full production package export
- do not expose secrets
- do not create public URLs

8. Add audit log events:
- video.runway.mock.attempted
- video.runway.mock.succeeded
- video.runway.real.attempted
- video.runway.real.succeeded
- video.runway.real.failed
- video.runway.rejected

9. Add regression tests:
- Runway availability requires root-admin
- availability returns booleans only
- secret canary never appears in any video response
- mock video generation works without API key
- real Runway route returns 412 without API key
- real Runway route requires confirm:true
- real Runway route requires root-admin
- generated video job is draft only
- generated video job has no publicUrl or signedUrl
- video job persists through storage adapter
- full export includes video job metadata but no secrets
- no Unreal command is sent
- no 4D command is sent
- SAFETY_ENVELOPE remains unchanged

10. Preserve all existing tests.

Do not add Convai, NVIDIA ACE, real Unreal sends, real Unreal imports, or real 4D sends in this task.