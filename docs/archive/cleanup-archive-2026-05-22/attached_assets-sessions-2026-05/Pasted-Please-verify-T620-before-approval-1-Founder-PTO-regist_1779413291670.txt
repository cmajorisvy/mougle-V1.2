Please verify T620 before approval:

1. Founder PTO registry includes exactly these new descriptors and they render on the Founder PTO admin page:
   - gateway_block_alert
   - audience_retention_failure
   - broadcast_sweep_failure
   - shorts_backlog
   - live_broadcast
   - audience_connector_rotation

2. Each service checks Founder PTO mode inside its existing send decision path, not after sending:
   - gateway-block-alert-service.ts
   - audience-retention-failure-alert-service.ts
   - broadcast-sweep-failure-alert-service.ts
   - shorts-backlog-alert-service.ts
   - live-broadcast-alert-service.ts
   - audience-connector-rotation-notifier.ts

3. When PTO-muted, the service does not send:
   - email
   - SMS
   - platform_alerts row
   - any external notification side effect

4. When PTO-muted, bumpFounderPtoSuppressedCount is called with the correct notifier key.

5. Existing per-service disable/snooze/dedup gates still work and PTO is only an additional OR-gate.

6. If the PTO service lazy import or DB read fails, the notifier logs the error and continues with the pre-existing behavior rather than crashing.

7. Dedup behavior is preserved across PTO:
   - gateway_block_alert preserves suppressedSinceLastFire / does not reset lastAlertAt incorrectly
   - audience_retention_failure preserves suppressedSince / suppressedCount
   - broadcast_sweep_failure keeps per-kind dedup state untouched
   - shorts_backlog once-per-crossing behavior remains intentional and documented
   - live_broadcast does not advance lastAlertAt / lastAlertCount during PTO
   - audience_connector_rotation leaves dedup state untouched

8. audience_connector_rotation records history reason pto_snoozed with errorMessage prefix pto_snoozed_until:.

9. After PTO ends, the first eligible alert sends normally and includes any intended suppressed-count context.

10. Existing tests for each touched service still pass:
   - gateway block alert tests
   - audience retention failure alert tests
   - broadcast sweep failure alert tests
   - shorts backlog alert tests
   - live broadcast alert tests
   - audience connector rotation notifier tests

11. Add at least one focused test per service, or one table-driven integration test covering all six notifier keys:
   - PTO enabled suppresses send
   - suppressed counter increments
   - PTO disabled allows normal send path

12. No schema/migration changes were introduced.

13. No new TypeScript errors were introduced by T620.