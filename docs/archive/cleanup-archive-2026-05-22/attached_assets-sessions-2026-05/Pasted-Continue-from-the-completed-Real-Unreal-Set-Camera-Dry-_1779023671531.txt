Continue from the completed Real Unreal Set-Camera Dry-Run feature.

Goal:
Add Real Unreal Set-Lighting Dry-Run support, mirroring the existing prepare-scene and set-camera dry-run patterns.

Context:
- Real Unreal prepare-scene dry-run already exists.
- Real Unreal set-camera dry-run now exists.
- Stage must be unreal_sandbox_approved.
- latest prepare-scene dry-run must have passed.
- realSendAllowed must remain false.
- bridge token must never be stored or returned.
- all routes must remain root-admin only.
- this is still dry-run only.

Hard constraints:
- Do not send real Unreal commands.
- Do not enable realSendAllowed.
- Do not call production Unreal unless dry-run endpoint only.
- Do not expose bridge token.
- Do not enable 4D hardware cues.
- Do not enable public publishing.
- Do not run db:push.
- Do not add migrations.
- Do not modify shared/schema.ts.
- Preserve all existing tests.

Create/extend contracts:
In shared/production-house.ts add:
- allowed lighting presets
- RealUnrealSetLightingRecordSchema
- related request/response types

Suggested lighting presets:
- newsroom_bright
- newsroom_breaking_red
- podcast_warm
- debate_neutral
- interview_soft
- market_watch_blue
- emergency_alert
- cinematic_low_key
- avatar_spotlight
- standby_dim

Storage:
In server/services/production-house-storage.ts add:
- realUnrealSetLightingDryRunHistory collection

Service:
In server/services/production-house-service.ts add:
- getRealUnrealSetLightingDryRunStatus()
- sendRealUnrealSetLightingDryRun()
- listRealUnrealSetLightingDryRunHistory()
- sanitized payload builder

Behavior:
- Send only sanitized set_lighting dry-run summary to:
  {UNREAL_BRIDGE_BASE_URL}/set-lighting/dry-run
- Do not include bridge token in stored payload or response.
- Preserve SAFETY_ENVELOPE.
- realSendAllowed must remain false.
- Require stage = unreal_sandbox_approved.
- Require latest prepare-scene dry-run passed.
- Optionally require latest set-camera dry-run passed if that is consistent with existing flow.
- Emit audit events for:
  status
  attempted
  passed
  failed
  rejected
  history

Routes:
In server/routes/production-house-routes.ts add:
- GET /api/admin/production-house/real-unreal/set-lighting/status
- POST /api/admin/production-house/real-unreal/set-lighting/send
- GET /api/admin/production-house/real-unreal/set-lighting/history

All routes:
- root-admin only
- CSRF protected where mutating
- no public routes
- no real Unreal send

UI:
Add a RealUnrealSetLightingDryRunPanel.

Mount it inside the existing real-unreal-dry-run section after set-camera.

Panel should include:
- lighting preset dropdown
- status
- send dry-run button
- latest response summary
- safety flags
- history list

Tests:
Add tests for:
- allowed lighting presets validate
- invalid lighting preset rejected
- status route root-admin only
- send route root-admin only
- send route rejects if stage is not unreal_sandbox_approved
- send route rejects if prepare-scene has not passed
- optional: rejects if set-camera has not passed, if implemented
- dry-run payload is sanitized
- bridge token is redacted
- realSendAllowed remains false
- SAFETY_ENVELOPE preserved
- history route returns sanitized records
- audit events emitted
- no real Unreal command is sent

Report:
Create:
docs/reports/CODEX_REAL_UNREAL_SET_LIGHTING_DRY_RUN_REPORT.md

Include:
- files changed
- routes added
- preset list
- gating behavior
- bridge-token redaction
- safety envelope behavior
- tests added
- no real Unreal send confirmation
- rollback notes
- remaining work before real Unreal sandbox execution

Validation:
- Run npm run check.
- Run npm run build.
- Confirm all tests pass.
- Confirm no db:push was run.
- Confirm shared/schema.ts is untouched.
- Confirm realSendAllowed remains false.
- Confirm bridge token is never serialized.