Now add a CSV exports metric tile to the AI Operations hero card and deep-link it to export history.

Context:
The project already has:

1. Founder dashboard AI Operations hero card:
   - client/src/components/admin/AiOpsHeroCard.tsx

2. Admin AI Retention page:
   - /admin/ai-retention

3. CSV export audit logging:
   - ai_export_events table
   - GET /api/admin/ai-export-events
   - CSV export history card on /admin/ai-retention

4. Existing AI Ops hero metric tiles:
   - Pending jobs
   - Running jobs
   - Failed 24h
   - Workers online
   - Stale/offline workers
   - Nudges
   - Cleanup eligible

Goal:
Add a compact “CSV exports today” metric tile to the AI Operations hero card and link it directly to the export history section.

Do not rewrite the dashboard.
Do not add Python changes.
Do not add Redis.
Do not add cron.
Do not expose Python directly to the frontend.
Do not add heavy dependencies.

Part A — Backend summary support

Check whether GET /api/admin/ai-ops/summary already exposes enough export audit data.

If it already exposes export counts:
- reuse the existing field.

If it does not:
- extend the existing AI ops summary service to include compact export metrics:
  - csvExportsToday
  - csvExportsLast24h if easier
  - failedCsvExportsLast24h if easy

Use ai_export_events as the source.
Keep it aggregate/count-only.
Do not expose filters or CSV contents.

Part B — Retention page anchor support

Update /admin/ai-retention so export history can be deep-linked.

Support:

/admin/ai-retention?section=exports

Behavior:
- scroll the CSV export history card into view
- preserve existing ?section=history behavior
- do not break retention history filters
- export history card should have a stable id or ref

Part C — AI Ops hero tile

Update AiOpsHeroCard.tsx:

Add metric tile:

Label:
CSV exports today

Value:
csvExportsToday or csvExportsLast24h

Link:
/admin/ai-retention?section=exports

Accessibility:
- data-testid="stat-hero-csv-exports"
- title="View CSV export history"
- aria-label="CSV exports today: {value}. View CSV export history"

Behavior:
- if backend field is unavailable, show "—" and do not misrepresent data
- keep existing tile layout readable
- if grid needs adjustment, update it carefully

Part D — UI validation

Run existing TypeScript checks/tests.

Confirm:
- AI Ops hero card still renders.
- CSV exports tile appears.
- tile links to /admin/ai-retention?section=exports.
- /admin/ai-retention?section=exports scrolls to export history.
- existing /admin/ai-retention?section=history behavior still works.
- no Python changes are required.

After changes, summarize:
- files created
- files modified
- backend metric added or reused
- destination URL
- section-scroll behavior
- validation results
- what remains for the next step