Now embed the AI operations summary into the existing Mougle admin/founder dashboard hero.

Context:
The project already has:

1. Admin AI Ops Summary:
   - GET /api/admin/ai-ops/summary
   - /admin/ai-ops

2. Admin AI Ops Notifications:
   - GET /api/admin/ai-ops/notifications

3. Local per-admin notification dismissal on /admin/ai-ops.

4. Daily AI Ops Snapshots:
   - POST /api/admin/ai-ops/snapshots
   - GET /api/admin/ai-ops/snapshots

5. Admin pages:
   - /admin/ai-jobs
   - /admin/ai-workers
   - /admin/ai-retention
   - /admin/ai-ops

Goal:
Show a compact AI operations health card in the existing admin/founder dashboard so admins can see AI system health at a glance.

Do not rewrite the dashboard.
Do not add backend persistence.
Do not add Redis.
Do not add cron.
Do not add email/SMS/push notifications.
Do not expose Python directly to the frontend.
Use the existing summary and notifications endpoints.

Part A — Locate dashboard

Find the existing admin/founder dashboard page.

Likely candidates:
- AdminDashboard
- FounderDashboard
- AdminHome
- Dashboard
- client/src/pages/admin/*
- client/src/pages/*Dashboard*

Use the existing app routing conventions.

Part B — Add compact AI Ops card

Add a compact section/card titled something like:

“AI Operations”

It should fetch:

- GET /api/admin/ai-ops/summary
- optionally GET /api/admin/ai-ops/notifications

Display:

1. Health status badge:
   - healthy
   - degraded
   - attention_needed

2. Key metrics:
   - pending jobs
   - running jobs
   - failed jobs last 24h
   - online workers
   - stale/offline workers
   - visible notification count if fetched

3. Health reasons:
   - show the top 1–3 reasons if not healthy

4. Quick links:
   - View AI Ops → /admin/ai-ops
   - AI Jobs → /admin/ai-jobs
   - Workers → /admin/ai-workers

Part C — Notification count

If fetching notifications:
- Count current notifications.
- If the local-dismissal hook can be reused safely, show visible notification count after local dismissals.
- If not, show total current notification count.
- Do not duplicate the full notification dismissal UI here.
- Keep the dashboard hero compact.

Part D — UI behavior

1. Use existing UI components and styling.
2. Show loading skeleton or compact loading state.
3. Show a small error state if summary fails.
4. Do not block the rest of the dashboard if AI ops summary fails.
5. Manual refresh is optional if the dashboard already has refresh behavior.
6. No destructive controls.
7. Admin-only; do not show to normal users.

Part E — Validation

Run existing checks/tests.

Confirm:
- dashboard route still loads.
- AI Ops card renders for admins.
- summary endpoint remains admin-gated.
- normal users do not see admin AI ops card.
- card links route correctly.
- local notification dismissals do not break the dashboard.
- no backend changes are required unless a small shared type/export is needed.
- no Python changes are required.

After changes, summarize:
- files created
- files modified
- dashboard page updated
- metrics shown
- notification count behavior
- validation results
- what remains for the next step