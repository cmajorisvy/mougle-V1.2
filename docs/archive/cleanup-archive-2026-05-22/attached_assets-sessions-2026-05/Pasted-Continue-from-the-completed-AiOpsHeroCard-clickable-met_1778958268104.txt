Continue from the completed AiOpsHeroCard clickable metrics task.

Goal:
Add a new "Cleanup eligible" metric tile to the AI Ops hero card, linking to /admin/ai-retention?section=history.

Important constraints:
- Keep this as a narrow frontend/admin enhancement.
- Do not change backend behavior unless the existing hero summary endpoint already exposes a retention/cleanup count.
- If no retention cleanup count is currently available from the existing summary data, do not invent one. Instead, show the tile only if a real field already exists, or add a TODO-safe fallback with value "—" and title "Retention cleanup count not exposed yet".
- Do not create new database tables.
- Do not add new dependencies.
- Do not modify unrelated files.
- Preserve all existing tests.
- Preserve existing data-testid values.

Tasks:
1. Inspect client/src/components/admin/AiOpsHeroCard.tsx and the data shape it receives.
2. Check whether the existing hero summary/admin AI ops summary response already includes a retention cleanup / cleanup eligible / retention backlog count.
3. If a real count exists:
   - Add a new metric tile labeled "Cleanup eligible".
   - Link it to /admin/ai-retention?section=history.
   - Use the same clickable Metric behavior added in the previous task.
   - Add a stable data-testid, for example stat-hero-cleanup-eligible.
   - Add an aria-label in the same format as existing metric tiles.
4. If no real count exists:
   - Do not modify backend in this task.
   - Add a guarded frontend placeholder only if it does not misrepresent data:
     label: "Cleanup eligible"
     value: "—"
     description/title: "Retention cleanup count not exposed yet"
     link: /admin/ai-retention?section=history
     data-testid: stat-hero-cleanup-eligible
   - Make sure this does not break type checking.
5. Confirm the tile layout still looks correct with the additional metric.
6. Run:
   npm run check

Acceptance criteria:
- Existing clickable metric tiles continue to work.
- New "Cleanup eligible" tile links to /admin/ai-retention?section=history.
- No backend/database changes unless a real existing field is already available and only frontend wiring is needed.
- TypeScript passes.
- All tests pass.
- Summarize exactly which files changed.