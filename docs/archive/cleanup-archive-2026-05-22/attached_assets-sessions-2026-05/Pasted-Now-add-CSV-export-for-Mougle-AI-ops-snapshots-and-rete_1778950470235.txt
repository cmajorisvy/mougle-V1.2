Now add CSV export for Mougle AI ops snapshots and retention cleanup history.

Context:
The project already has:

1. AI Ops Summary:
   - GET /api/admin/ai-ops/summary
   - /admin/ai-ops

2. AI Ops Snapshots:
   - POST /api/admin/ai-ops/snapshots
   - GET /api/admin/ai-ops/snapshots
   - ai_ops_snapshots table

3. Retention cleanup history:
   - GET /api/admin/ai-retention/runs
   - ai_retention_runs table

4. Admin Retention UI:
   - /admin/ai-retention

5. Founder dashboard AI Operations hero card.

Goal:
Allow admins/founders to export AI ops history as CSV for compliance, board reporting, and operational review.

Do not rewrite the app.
Do not add external reporting services.
Do not add cron.
Do not expose Python directly to the frontend.
Do not add heavy dependencies.
Keep exports admin-only.

Part A — Backend CSV endpoints

Add admin-only CSV export endpoints:

1. GET /api/admin/ai-ops/snapshots.csv

Supports filters:
- since
- until
- healthStatus
- limit
- offset

Exports recent AI ops snapshots.

2. GET /api/admin/ai-retention/runs.csv

Supports filters:
- dryRun
- status
- adminId
- since
- until
- limit
- offset

Exports retention cleanup run history.

Security:
- requireRootAdmin
- unauthenticated users cannot export
- normal users cannot export
- do not expose secrets
- do not expose raw payloads
- set correct headers:
  - Content-Type: text/csv; charset=utf-8
  - Content-Disposition: attachment; filename="mougle-ai-ops-snapshots-YYYY-MM-DD.csv"

Part B — CSV formatting

Add a small utility if useful, for example:

server/utils/csv.ts

Requirements:
- quote fields safely
- escape double quotes
- flatten compact JSON fields into readable strings
- keep column order stable
- avoid external CSV dependency unless the project already uses one

Snapshot CSV columns should include:

- snapshotId
- snapshotDate
- generatedByAdminId
- healthStatus
- healthReasons
- totalJobsLast24h
- pendingJobs
- runningJobs
- succeededJobsLast24h
- failedJobsLast24h
- rejectedJobsLast24h
- staleRunningJobs
- totalWorkers
- onlineWorkers
- staleWorkers
- offlineWorkers
- unhealthyWorkers
- busyWorkers
- idleWorkers
- auditEventsLast24h
- lastCleanupAt
- lastCleanupStatus
- rowsDeletedLastCleanup
- rowsDeletedLast7d
- criticalNotifications
- warningNotifications
- infoNotifications
- createdAt

Retention run CSV columns should include:

- runId
- adminId
- dryRun
- status
- completedRetentionDays
- failedRetentionDays
- eventRetentionDays
- workerStaleRetentionDays
- eligibleCompletedJobs
- eligibleFailedJobs
- eligibleAuditEvents
- eligibleStaleWorkers
- deletedCompletedJobs
- deletedFailedJobs
- deletedAuditEvents
- deletedStaleWorkers
- error
- startedAt
- completedAt
- createdAt

Part C — Frontend UI

Update:

1. /admin/ai-ops
   - Add “Export snapshots CSV” button near the AI Ops Trends card.
   - Preserve current filters if the page has any, otherwise export recent snapshots.

2. /admin/ai-retention
   - Add “Export cleanup history CSV” button near the Cleanup History panel.
   - Preserve current filters if present, otherwise export recent runs.

UI behavior:
- clicking opens/downloads the CSV endpoint
- show a clear label
- no destructive action
- no polling needed
- loading state optional

Part D — Validation

Run existing checks/tests.

Confirm:
- unauthenticated users cannot access CSV endpoints.
- normal users cannot access CSV endpoints.
- admin CSV endpoint returns text/csv.
- CSV has expected headers.
- CSV escapes commas/quotes/newlines correctly.
- UI buttons route to the correct endpoints.
- no Python changes are required.

After changes, summarize:
- files created
- files modified
- CSV endpoints added
- CSV columns exported
- UI buttons added
- security behavior
- validation results
- what remains for the next step