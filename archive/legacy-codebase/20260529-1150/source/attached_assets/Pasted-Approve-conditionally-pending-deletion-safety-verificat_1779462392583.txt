Approve conditionally, pending deletion-safety verification.

Task #776 is acceptable in principle because it mirrors the production-asset permanent-delete pattern and limits deletion to archived avatar rigs only.

Before final approval, please verify:

1. Server-side archived-only enforcement
- DELETE /api/admin/production-rigs/:id rejects non-archived rigs.
- Non-archived delete returns 409 rig_not_archived.
- The client button being hidden is not the only protection.

2. Confirmation + reason validation
- DELETE body requires confirm: true.
- reason is required and non-empty.
- Invalid/missing confirm or reason returns 400.
- Client prompts for name match and reason before calling DELETE.

3. Auth and CSRF
- DELETE route uses requireAdmin.
- Non-admin/anonymous requests are rejected.
- Unsafe DELETE method remains CSRF-protected by existing middleware.

4. Object/delete transaction ordering
- Object bytes are deleted first.
- If object deletion fails, DB row remains for retry and route returns 502.
- If object deletion succeeds, deleteArchivedRig runs DB transaction.
- DB transaction deletes audit rows and rig row together.

5. Audit/moderation logging
- moderation_logs row is written BEFORE cascade/delete.
- contentType=production_rig.
- category=rig_deletion.
- severity=high.
- actor and reason are persisted.
- Failed DB transaction does not leave a partially deleted rig row.

6. Client behavior
- Delete button appears only when status === "archived".
- Button data-testid is button-permanently-delete.
- Success navigates back to /admin/3d-rigs.
- Inline error displays on failure.
- Non-archived rigs have no delete button.

7. No unintended scope creep
- No schema/migration changes unless already required by existing tables.
- No Production House/R3F behavior changed.
- No upload/import/approval behavior changed.
- No publicUrl/signedUrl/realSendAllowed/executionEnabled behavior changed.

8. TypeScript/tests
- No new TypeScript errors in:
  - server/services/production-rig-storage.ts
  - server/storage.ts
  - server/routes/admin/production-rigs.ts
  - client/src/pages/admin/3d-rigs/RigDetail.tsx
- Any existing unrelated errors remain documented separately.

If all checks pass, Task #776 is approved.

Follow-up suggestion:
Add a later bulk-delete archived avatar rigs task only after this per-rig delete path is verified stable.