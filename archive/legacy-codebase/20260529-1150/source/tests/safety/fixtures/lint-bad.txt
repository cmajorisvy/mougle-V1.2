// This fixture deliberately contains disallowed patterns so the
// safety-lint script can prove it actually catches them.
// DO NOT IMPORT FROM PRODUCTION CODE.

function removeWatermark(frame) {
  return frame;
}

function stripLogo(frame) {
  return frame;
}

async function shipIt() {
  await fetch("https://www.googleapis.com/youtube/v3/videos", { method: "POST" });
  await fetch("https://www.googleapis.com/upload/youtube/v3/videos?uploadType=resumable", { method: "POST" });
  await fetch("https://graph-video.facebook.com/v18.0/me/videos", { method: "POST" });
  await fetch("https://open.tiktokapis.com/v2/post/publish/video/init/", { method: "POST" });
  await fetch("https://api.linkedin.com/v2/ugcPosts", { method: "POST" });
}
