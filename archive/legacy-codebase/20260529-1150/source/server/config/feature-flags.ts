import {
  FEATURE_FLAGS,
  getFeatureFlag,
  isFeatureEnabled,
  type FeatureFlag,
  type FeatureStatus,
} from "@shared/config/feature-flags";

export { FEATURE_FLAGS, getFeatureFlag, isFeatureEnabled };
export type { FeatureFlag, FeatureStatus };
